# Box server site for nginx (Debian 13). Replace box.example.org with your domain.
# Installed to /etc/nginx/sites-available/box-server.conf, see docs/betrieb-debian13.md.
# sites-enabled/* is included inside the http {} block, so zones and formats can live here.

# Never log query strings: /api/v1/pairing/poll carries the poll token (SPEC §7.1),
# /invite carries the invitation token.
log_format box_noquery '$remote_addr - [$time_local] "$request_method $uri $server_protocol" '
                       '$status $body_bytes_sent $request_time "$http_user_agent"';

limit_req_zone $binary_remote_addr zone=box_pairing:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=box_login:10m rate=20r/m;

upstream box_server {
    server unix:/run/box-server/api.sock;
    keepalive 16;
}

server {
    listen 80;
    listen [::]:80;
    server_name box.example.org;
    access_log /var/log/nginx/box-server.access.log box_noquery;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name box.example.org;

    ssl_certificate     /etc/letsencrypt/live/box.example.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/box.example.org/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:box_ssl:10m;
    ssl_session_timeout 1d;

    add_header Strict-Transport-Security "max-age=31536000" always;
    access_log /var/log/nginx/box-server.access.log box_noquery;

    # Keep in line with BOX_SERVER_MAX_UPLOAD_MB (per file) plus form overhead.
    client_max_body_size 1100m;
    client_body_timeout 300s;

    proxy_http_version 1.1;
    proxy_set_header Connection "";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 300s;

    location / {
        proxy_pass http://box_server;
    }

    location /api/v1/pairing/ {
        limit_req zone=box_pairing burst=20 nodelay;
        limit_req_status 429;
        proxy_pass http://box_server;
    }

    location = /login {
        limit_req zone=box_login burst=10 nodelay;
        limit_req_status 429;
        proxy_pass http://box_server;
    }

    # Asset delivery after the app's permission check (X-Accel-Redirect, SPEC §3.8).
    # "internal": never reachable directly from outside.
    location ~ "^/_assets/(?<p1>[0-9a-f]{2})/(?<p2>[0-9a-f]{2})/(?<sha>[0-9a-f]{64})\.opus$" {
        internal;
        alias /var/lib/box-server/assets/$p1/$p2/$sha.opus;
        types { }
        default_type "audio/ogg; codecs=opus";
        etag off;
        add_header ETag "\"$sha\"" always;
        add_header Cache-Control "private, max-age=31536000, immutable" always;
    }
    location ~ "^/_assets/(?<p1>[0-9a-f]{2})/(?<p2>[0-9a-f]{2})/(?<sha>[0-9a-f]{64})\.jpg$" {
        internal;
        alias /var/lib/box-server/assets/$p1/$p2/$sha.jpg;
        types { }
        default_type image/jpeg;
        etag off;
        add_header ETag "\"$sha\"" always;
        add_header Cache-Control "private, max-age=31536000, immutable" always;
    }
}
