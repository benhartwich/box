[project]
name = "box-protocol"
version = "0.3.0"
description = "Box device protocol v1 message models (SPEC v0.3), shared by agent and server"
requires-python = ">=3.13,<3.14"
dependencies = ["pydantic>=2.11"]

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"
